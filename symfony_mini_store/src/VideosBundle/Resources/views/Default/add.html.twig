{% extends "::base.html.twig" %}

{% block body %}
    
    <style>
        label {
            display: block;
            width: 200px;
            float: left;
        }
        input {
            width: 200px;
        }
        textarea {
            width: 200px;
        }
    </style>
    
    <h1>Add a video</h1>
    <div style="margin: 20px;">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
    <div style="margin: 20px;">
        <a href="{{ path("videos_homepage") }}">Return to list</a>
    </div>
{% endblock %}
